# Pi configuration package

A Pi package with 14 local skills, question UI, tick scheduler, ponytail, subagents, and a project-local Git status widget.

From this directory, set up once:

```sh
npm install
pi install .
```

Pi extensions run with full system access. Review extensions and skills before use.
